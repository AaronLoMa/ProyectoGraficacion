import 'package:flutter/material.dart';

class EncursoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Página de Cuidados'),
      ),
      body: Container(
      ),
    );
  }
}
